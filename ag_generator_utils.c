/*
 * ag_generator_utils.c
 *
 *  Created on: Jan 16, 2016
 *      Author: john-hale
 */

#include<stdio.h>
#include<stdlib.h>

#include "identifier.h"
#include "network_state.h"
#include "exploit.h"
//#include "globals.h"
#include "ag_generator_utils.h"

struct bound_param_lists *exploit_asset_bindings(struct asset_hashtable *a_ht, struct exploit *e) {
/*
 *
current_asset = asset_ht->head;
current_param = bp_l->head;
while (current_param != NULL) {
  Bind(current_asset,current_param)
  current_param = current_param->next;
  }
 	 for (j = 0 to params - 1)
 	 	 bind(bp_list,asset[i]/param[j])
 	 next bp list
*/
    return NULL;
}
