#ifndef ODOMETER_HPP
#define ODOMETER_HPP

template<typename H, typename... T>
void evaluate(H head, T... tail) {
    evaluate(tail...);
}

void create_permutation(int n, int k) {
    
}

#endif //ODOMETER_HPP
